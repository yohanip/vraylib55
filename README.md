this project successfully compiled RAYLIB+NUKLEAR to web!
yeayy!!

to generate v "bindings" we use rl.py

run with emrun build-emscripten

**NOTE**

unfortunately for emscripten we need to specify -gc none and -d no_backtrace